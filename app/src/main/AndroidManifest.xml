<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.smokeless.smokelessnewc">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".InfoKesehatan" />
        <activity android:name=".InfoKeuangan" />
        <activity android:name=".InputHarian" />
        <activity android:name=".InputPertama" />
        <activity android:name=".Riwayat" />
        <activity android:name=".RiwayatBulanan" />
        <activity android:name=".RiwayatMingguan"></activity>
    </application>

</manifest>